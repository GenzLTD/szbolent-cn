# 本机服务清单（CATALOG）

> 机器：阿里云 ECS `47.115.168.107`（Alibaba Cloud Linux 3 · 2 vCPU · ~3.5 GiB）
> 与 docs 仓《资源-主体-服务对账表》对齐；**本表只管本机**。

## 一、服务

| # | 服务 | 容器 | 端口（收内网） | 主体 | 备注 |
|---|---|---|---|---|---|
| 1 | 内容线（门户/博客） | `bolent_wp` + `bolent_wp_mysql` | `127.0.0.1:8080` | szbolent.cn（个人备案） | WordPress |
| 2 | 实验向量库 | `pgvector` | `127.0.0.1:5433` | 组织 / 实验 | 收内网，**非组织结论** |
| 3 | 宿主 nginx | 系统服务 | `80` | — | 反代 → WP |

## 二、安全组（阿里云控制台 · 入方向）

仅开放三项，其余端口由安全组挡在公网外：

| 端口 | 用途 |
|---|---|
| `22` | SSH |
| `80` | HTTP |
| `8010` | Tatha API（待对账） |

> 容器层再收一层（`127.0.0.1:` 绑定）：**安全组 + 容器绑定双保险**。

## 三、已知跨机依赖（待解耦）

| 依赖 | 现状 | 解耦方向 |
|---|---|---|
| 门户 `/v1/` → `api.genz.ltd`（腾讯云） | nginx 反代 | 本地化后端，或去掉反代 |

> 目标：本机部署/运行**不依赖其他服务器的运行状态**。该依赖属遗留，逐项消解。

## 四、归属与治理

- 本仓只声明「本机部署」，不含产品代码；产品演进在各产品仓。
- 资源↔主体↔服务的完整对账以 docs 仓《资源-主体-服务对账表》为准。
